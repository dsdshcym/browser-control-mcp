{
  "name": "@browser-control/extension",
  "version": "2.0.0",
  "main": "dist/background.js",
  "scripts": {
    "build": "esbuild background.ts --bundle --outfile=dist/background.js && esbuild options.ts --bundle --outfile=dist/options.js",
    "pack": "npm run build && node scripts/pack.js",
    "test": "jest"
  },
  "license": "MIT",
  "description": "Browser Control CLI Firefox Extension",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "esbuild": "0.25.1",
    "jest": "^30.0.4",
    "jest-environment-jsdom": "^30.0.4",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.2",
    "@types/firefox-webext-browser": "^120.0.4",
    "@browser-control-mcp/common": "*"
  }
}

{
  "name": "browser-control-mcp-native-host",
  "version": "1.0.0",
  "description": "Native messaging host for Browser Control MCP extension",
  "main": "dist/host.js",
  "bin": {
    "browser-control-mcp-host": "dist/host.js"
  },
  "scripts": {
    "build": "esbuild src/host.ts --bundle --platform=node --outfile=dist/host.js --format=cjs && chmod +x dist/host.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "nx": {}
}

{
  "name": "browser-control-mcp",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix extension && npm install --prefix native-host",
    "build": "nx run-many --target=build --all --parallel"
  },
  "devDependencies": {
    "nx": "22.3.3"
  }
}
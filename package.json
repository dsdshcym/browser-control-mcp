{
  "name": "browser-control-cli-monorepo",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "common",
    "extension",
    "native-host",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}

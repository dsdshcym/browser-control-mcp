{
  "name": "browser-control-cli-monorepo",
  "version": "1.5.0",
  "private": true,
  "workspaces": [
    "common",
    "extension",
    "native-host"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}
